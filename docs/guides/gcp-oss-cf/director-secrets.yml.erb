<%
 raise "Expected environment variable 'common_secret' to be set" if ENV['common_secret'] == nil || ENV['common_secret'] == ''
%>

routing-cf-client-secret: <%= ENV['common_secret'] %> # UAA client secret for routing client
routing-cf-nats-password: <%= ENV['common_secret'] %> # Password for Cloud Foundry NATS
